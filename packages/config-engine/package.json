{
  "name": "@perfeccity/config-engine",
  "version": "1.0.0",
  "private": true,
  "description": "Configuration Engine (rules R1–R9, quantity formulas) — isolated, unit-tested independently",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest --run"
  }
}
